name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World for Github Action"
      - name: Print goodbye
        run: echo "Goodbye World for Github Actions"
